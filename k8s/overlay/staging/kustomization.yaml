resources:
  - ../../base
  - namespace.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
- name: example-app
  literals:
    - STAGE="STAGING"
    - HOST_NAME="$(hostname)"

namespace: example-app-staging